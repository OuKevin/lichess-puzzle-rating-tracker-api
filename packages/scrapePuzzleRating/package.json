{
  "name": "scrape-puzzle-rating",
  "version": "1.0.1",
  "description": "API to scrape ratings from Lichess Api",
  "main": "index.js",
  "scripts": {
    "build": "NODE_ENV=production && (mkdir dist || rm dist/* || rm -rd dist ) && webpack && cd dist && zip -qr archive.zip index.js",
    "lint": "eslint \"@(src)/**/*.js?(x)\" --fix;"
  },
  "repository": {
    "type": "git",
    "url": "https://https://github.com/OuKevin/lichess-puzzle-rating-tracker-api",
    "directory": "packages/fetchAndSavePuzzleRating"
  },
  "author": "Kevin Ou",
  "devDependencies": {
    "@babel/core": "^7.11.6",
    "@babel/preset-env": "^7.11.5",
    "aws-sdk": "^2.784.0",
    "dotenv": "~8.2.0",
    "eslint": "^7.12.1",
    "webpack": "^4.44.2",
    "webpack-cli": "^3.3.12"
  },
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.1"
  }
}
